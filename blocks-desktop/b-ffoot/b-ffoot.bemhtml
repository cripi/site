block b-ffoot {
    content: {
        elem: 'inner',
        mix: [{ block: 'i-clearfix' }],
        content: {
            elem: 'ololo',
            content: this.ctx.content
        }
    }

    default: {
        apply( this.ctx.content.attrs = {'type': 'ololo'}, '')
    }
}
